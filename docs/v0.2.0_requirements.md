# Backgound Information
- version = "0.1.0" Scripts: `garage/redshift-ai-native-ops/scripts/jack-cli`
- DB 架構是 redshift cluster with 2 nodes
  
---

# 第一次修改

## 初始設定
1. 僅保留原有軟體設計架構，拋棄 **所有的已建構功能**，進行重新設計
   - docs:
     - `garage/redshift-ai-native-ops/docs/redshift_jack_jobs.md` 平日營運指令集
     - `garage/redshift-ai-native-ops/docs/redshift_jack_feedback.md` 版本回饋與建議
   - 原本設計過為複雜且不必要，請根據實際需求進行重構，文件參考優先
      1. 平日營運指令集
      2. 版本回饋與建議
      3. 原版本設計
   - 須改善上版本缺陷
     - 考量實際需求，保留必要輸出參數
     - 條列計畫進行所需文件，包括但不限於以下條列，我可以在 db 端產出補充，請以可執行的 sh 交付
       - DB 權限確認 
       - 所需 DB Tables & Columns 確認

2. 新增預設 .env.example
   REDSHIFT_PASSWORD 檢視連線程式，須容許密碼中有特殊字元

   ```
   # Redshift
    export REDSHIFT_HOST="singapore-prd-datalakehouse-redshift-cluster-1.cuswnymapmyu.ap-southeast-1.redshift.amazonaws.com"
    export REDSHIFT_PORT="5439"
    export REDSHIFT_DATABASE="dwa"
    export REDSHIFT_USER="datalake_op"
    export REDSHIFT_PASSWORD=""

    # AWS Bedrock
    export AWS_DEFAULT_REGION="ap-southeast-1"
    export BEDROCK_MODEL="apac.amazon.nova-pro-v1:0"
    #export BEDROCK_MODEL="apac.anthropic.claude-sonnet-4-20250514-v1:0"
    ```
3. 移除 'onboarding.sh' 'test_*.sh'

4. 我沒有辦法拿到 db admin 權限，只能擁有部分 table 閱讀權限；另外，禁止任何會影響 db 效能的操作，比如說 vacuume analyze，建議中提及是可以，但是要加上註明；嚴禁 cli sql 執行
5. 我們會建立一些監控結果的 table ，確保這些放在我們指令群組
6. 記得修改 README 跟產出一份 handoff document，文件開頭請給一段文字敘述這個專案，讓其他 AI 未來可以快速接手開發
7. 你可以先提供我 scripts 讓我在 DB 端執行確認，當作補充資料；但請一次提供你計畫中需要我確認的 script
   - `garage/redshift-ai-native-ops/docs/redshift_jack_jobs.md` 平日營運指令集
   - `garage/redshift-ai-native-ops/docs/redshift_jack_feedback.md` 版本回饋與建議
----

1. 你可以先提供我 scripts 讓我在 DB 端執行確認，當作補充資料；但請一次提供你計畫中需要我確認的 script
   - `garage/redshift-ai-native-ops/docs/redshift_jack_jobs.md` 平日營運指令集
   - `garage/redshift-ai-native-ops/docs/redshift_jack_feedback.md` 版本回饋與建議
2. 你是說 TABLE_HEALTH_EXPLAIN_PROMPT？建議中提及是可以，但是要加上註明；嚴禁 cli sql 執行
3. 你可以從我提供的文件中掃描，datalake_op 無法建立 table，僅能取用已建好的 mcd 專用監控 table，同 1. 也可以提供 sh 讓我補充文件

